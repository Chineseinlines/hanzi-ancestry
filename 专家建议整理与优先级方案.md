# 专家建议整理与优先级实施方案

> 基于对 `hanzi-dict.json`、`shuowen.json`、类型系统、UI组件、游戏系统的全面审查，结合专家反馈整理。

---

## 一、当前已发现的具体问题

### 1.1 六书分类错误（严重）

**数据来源**: `zhishi_check.txt` 已验证

| 问题 | 数量 | 说明 |
|---|---|---|
| 指事字被标为 `ideographic`（会意） | **54个** | 如：上、下、本、末、刃、旦、中、天、夫… |
| 指事字无分类标记（空） | **15个** | 如：丑、屯、六、七、十、午、臣… |
| 指事字被标为 `pictographic`（象形） | **10个** | 如：立、九、世、元、片、示、南… |
| 指事字被标为 `pictophonetic`（形声） | **3个** | 牟、尤、象 |
| 不在数据库中 | **2个** | 必、言 |

**根因**: `hanzi-dict.json` 中 `etymology.type` 字段大量使用 `"ideographic"` 作为默认值，`"indicative"` 类型虽在 TypeScript 类型系统中已定义（`src/data/types.ts:2`），但数据源未正确使用。

### 1.2 术语翻译问题

- 代码中 "ideographic component" 对应的中文应为「表意构件」或「意符」
- 专家建议统一使用 **"semantic component"** 替代 "ideographic component"
- 涉及文件：`DecompositionGraph.tsx`、`CharacterDetail.tsx`、`types.ts`

### 1.3 数据库覆盖缺口

- 繁体字形数据：`simpToTrad` 映射已加载但仅在后台使用，前端展示不足
- 古文字形图片：甲骨文/金文/小篆通过本地 SVG 和 GlyphWiki 加载，但覆盖率参差不齐
- 说文解字数据：已加载 `shuowen.json`（3.4MB），但仅在 CharacterDetail 页面展示，未在探索/搜索流程中体现

---

## 二、专家建议整理（按可实施优先级排序）

### P0 🔴 紧急修复 — 数据层改正

#### P0-1: 修正 84 个指事字的六书分类

**改动范围**: `public/hanzi-dict.json`（约 1.9MB，需批量修改 etymology.type 字段）

**具体操作**:
```
54 个 ideographic → indicative: 非、太、卑、一、公、卓、卅、八、不、乒、章、乓、乏、音、甘、竟、卡、叉、即、尺、夫、五、本、至、亡、下、凶、旦、未、交、幻、牵、函、亦、中、王、朱、二、末、天、丹、四、上、寸、三、引、廿、曰、司、克、血、半、爻、刃

15 个 (empty) → indicative: 丑、屯、兮、丨、丩、齐、亟、六、午、七、十、臣、派、乎、永

10 个 pictographic → 复核/修正: 立、九、世、尹、元、片、示、南、少、卒

3 个 pictophonetic → 复核/修正: 牟、尤、象
```

**预计工时**: 2-3小时（编写脚本批量修改 + 人工复核）

#### P0-2: 术语统一 — "ideographic component" → "semantic component"

**改动范围**:
- `src/data/types.ts:2`: `EtymologyData.type` 注释
- `src/components/DecompositionGraph.tsx:42,50`: `IDEOGRAPHIC_COLOR` 标签和 LEGEND
- `src/pages/CharacterDetail.tsx:79-87`: `getEnglishSummary` 中的翻译文本

**预计工时**: 1小时

---

### P1 🟠 高优先级 — 形声字增强（核心学术价值）

> 专家核心观点：约90%的汉字是形声字，系统应对此做更深度的展示。

#### P1-1: 形声字语义关系分级标注（8级）

**新增数据字段**（在 `EtymologyData` 或新建类型中）:

```typescript
// 新增类型
export type SemanticRelationLevel = 1 | 2 | 3 | 4 | 5 | 6 | 7 | 8;

export interface SemanticRelation {
  level: SemanticRelationLevel;
  label: string;     // 中文标签
  enLabel: string;   // 英文标签
  description: string;
}

// 8级语义关系
const SEMANTIC_LEVELS: SemanticRelation[] = [
  { level: 1, label: '同义', enLabel: 'Identical', 
    description: '汉字与表意构件意义完全相同。例如，"树"与意旁"木"都指树木一类。' },
  { level: 2, label: '种属', enLabel: 'Taxonomic',
    description: '形声字所表事物属于意旁所表事物的种类。例如，"鸥"是一种"鸟"。' },
  { level: 3, label: '直接相关', enLabel: 'Directly Related',
    description: '汉字意义与表意构件意义直接相关。例如，"河"与"水"直接相关。' },
  { level: 4, label: '间接相关', enLabel: 'Indirectly Related',
    description: '汉字意义与表意构件意义间接相关。例如，"炒"与意旁"火"有联系。' },
  { level: 5, label: '引申直接相关', enLabel: 'Extended Direct',
    description: '汉字引申义与表意构件意义直接相关。例如，"请"（拜访→邀请）与"言"。' },
  { level: 6, label: '引申间接相关', enLabel: 'Extended Indirect',
    description: '汉字引申义与表意构件意义间接相关。例如，"距"（鸡趾→距离）与"足"。' },
  { level: 7, label: '不相关', enLabel: 'Unrelated',
    description: '汉字意义与表意构件意义不相关。例如，"软"与部首"车"无关系。' },
  { level: 8, label: '难定义', enLabel: 'Undefinable',
    description: '由于简化或其他原因难以定义表意构件关系。例如，"丛"。' },
];
```

**UI 展示**: 在 CharacterDetail 页的形声字区域新增一个「语义关系等级」徽章，附带解释 tooltip。

**预计工时**: 3-4小时（类型定义 + UI组件 + 数据标注，首批覆盖500个高频形声字）

#### P1-2: 形声字声旁可靠性6级标注

**新增数据字段**:

```typescript
export type PhoneticRelationLevel = 1 | 2 | 3 | 4 | 5 | 6;

export interface PhoneticRelation {
  level: PhoneticRelationLevel;
  label: string;
  enLabel: string;
  description: string;
}

const PHONETIC_LEVELS: PhoneticRelation[] = [
  { level: 1, label: '完全相同', enLabel: 'Identical',
    description: '汉字读音与声旁完全相同，包括声调。例如，"清"/qīng/ 和声旁"青"/qīng/。' },
  { level: 2, label: '同音节异调', enLabel: 'Same Syllable, Different Tone',
    description: '汉字发音与声旁音节相同，声调不同。例如，"请"/qǐng/ 和声旁"青"/qīng/。' },
  { level: 3, label: '同韵母', enLabel: 'Same Final',
    description: '形声字韵母与声旁韵母相同，声母不同。例如，"精"/jīng/ 同"青"/qīng/。' },
  { level: 4, label: '同声母', enLabel: 'Same Initial',
    description: '汉字发音开头与声旁相同，末尾不同。例如，"结"/jié/ 同"吉"/jí/。' },
  { level: 5, label: '完全不同', enLabel: 'Completely Different',
    description: '汉字发音与声旁完全不一样。例如，"猜"/cāi/ 同"青"/qīng/。' },
  { level: 6, label: '多音字', enLabel: 'Multiple Pronunciations',
    description: '汉字或声旁不止一个发音。例如，"朴"/pò/或/piáo/。' },
];
```

**与现有 `phoneticRating.ts` 的关系**: 现有的3色系统（绿/黄/红）可作为简化版，6级系统作为详细版，在「高级信息」面板中展示。

**预计工时**: 2-3小时（类型定义 + 与现有 phoneticRating 整合 + UI）

#### P1-3: 形声字构件组合可视化

**需求**: 在 DecompositionGraph 中明确展示：
- 表意构件（semantic component / 形旁）— 蓝色圆圈
- 示音构件（phonetic component / 声旁）— 橙色菱形

**当前状态**: 已部分实现。`DecompositionGraph.tsx` 已有 semantic/phonetic 节点类型区分和不同形状/颜色。需要增强：
- 添加更明确的关系连线标注（如 "表意 →"、"示音 →"）
- 在 tooltip 中展示语义关系等级和声旁可靠性等级

**预计工时**: 2-3小时

---

### P2 🟡 中优先级 — 数据丰富与字形增强

#### P2-1: 繁体字形前端展示

**当前状态**: 
- `simpToTrad` 映射已加载（`simp-trad-map.json` → `loadSimpTradMap()`）
- `HanziEntry` 类型已有 `traditional` 字段
- CharacterDetail 仅在小徽章中显示 "字形 & 字源数据来自繁体: X"

**需增强**:
- 在所有字符展示处（搜索结果、详情页、关联字列表）显示繁简对照
- 为繁体字添加独立的详情页（或同一页面切换繁简）
- 在 GlyphEvolution 中显示繁体字的古文字形

**预计工时**: 4-5小时

#### P2-2: 扩展古文字形覆盖

**当前状态**:
- `GlyphEvolution.tsx` 支持5种书体（甲骨文/金文/小篆/隶书/楷书）
- 本地 SVG 字形目录：`public/glyphs/{oracle,bronze,seal,clerical}/`
- 回退到 GlyphWiki API
- 楷书使用现代字体

**需增强**:
- 增加大篆（籀文）书体阶段
- 批量获取/生成高频字的古文字形 SVG
- 添加 ctext.org / hanziyuan.net 的外部链接引用

**预计工时**: 6-8小时（主要是字形数据收集整理）

#### P2-3: 说文解字数据完善

**当前状态**: 
- `shuowen.json` (3.4MB) — 通过 `getShuowen()` 加载
- CharacterDetail「知识卡片」标签展示结构/六书分类
- GlyphEvolution 面板展示原文（可展开）

**需增强**:
- 在搜索/探索流程中也展示说文分类标签
- 每个字符页面添加 "在 ctext.org 查看" 的外部链接
- 增加段玉裁《说文解字注》的注释引用

**预计工时**: 3-4小时

---

### P3 🟢 中长优先级 — 现代理论融合

#### P3-1: 引入王宁《汉字构形学》分类体系

**需求**: 在传统六书之外，增加现代汉字构形学分类维度。

**王宁理论核心概念**:
- 构件（部件）分为：表形构件、表义构件、示音构件、标示构件、记号构件
- 结构模式：平面结构 / 层次结构
- 构形模式：全功能零合成、标形合成、标义合成、会形合成、形义合成、义音合成…

**实施方案**:
1. 在 `EtymologyData` 中新增 `modernType` 字段
2. 在 CharacterDetail 新增一个「现代构形学分析」区域
3. 与传统六书并行展示，提供双重解读视角

**预计工时**: 8-10小时（需要汉字构形学专家参与标注）

#### P3-2: 构建"视觉语法"规则引擎

**需求**: 定义汉字功能构件的正确/错误空间组合规则。

**核心规则类型**:
```
1. 位置规则: 形旁通常在左，声旁通常在右（如：河 = 氵+可）
2. 包围规则: 辶/囗/门 等框架部首必须半包围或全包围（如：过、国、问）
3. 上下规则: 艹/⻊/雨 等在上，灬/心/皿 等在下（如：花、热、想）
4. 变形规则: 亻(人)、扌(手)、氵(水)、忄(心) 等只能出现在左旁
5. 排斥规则: 某些构件组合在结构上不可能（如：两个全包围部首）
```

**数据模型**:
```typescript
interface SpatialRule {
  component: string;           // 构件
  allowedPositions: ('left' | 'right' | 'top' | 'bottom' | 'enclose' | 'semi-enclose')[];
  forbiddenPositions: ('left' | 'right' | 'top' | 'bottom' | 'enclose' | 'semi-enclose')[];
  whenWith?: string[];         // 与其他特定构件的空间约束
}
```

**预计工时**: 10-15小时（规则库构建 + 验证引擎 + UI）

---

### P4 🔵 远期 — 创新型功能

#### P4-1: 假字学习游戏（Pseudo-Character Game）

**需求**: 用实际不存在的"假字"测试用户对形声字结构逻辑的掌握。

**游戏设计**:
1. 系统按形声字空间规则生成一个「假字」（如：合理组合 氵 + 青 → 一个不存在的字）
2. 给出4个虚构释义 + 4个虚构读音选项
3. 只有一个在语义上与表意构件对应、在读音上与示音构件对应
4. 用户猜测 → 系统反馈对错 + 解析

**技术要点**:
- 假字生成器：从常用形旁集合 × 常用声旁集合中随机组合
- 验证假字不存在于真实汉字库中
- 干扰选项生成：用不同语义/语音关系构造

**预计工时**: 12-16小时（全新游戏模式）

#### P4-2: 用户自主造字工坊

**需求**: 用户自由组合构件构建汉字，系统实时验证视觉语法。

**功能设计**:
1. 拖拽式构件选择器（形旁库 + 声旁库 + 基础构件库）
2. 实时空间验证（是否违反构字规则）
3. 语义+语音匹配度预估
4. 与已有汉字的相似度检测
5. 生成「造字档案」可分享

**预计工时**: 20-25小时（大型新功能）

---

## 三、实施路线图

```
第1周 (P0)
├── P0-1: 修正84个指事字分类 + 批量数据清洗脚本
├── P0-2: 术语统一 "ideographic" → "semantic component"
└── 验证：回归测试所有受影响页面

第2-3周 (P1)
├── P1-1: 8级语义关系标注（首批500字）
├── P1-2: 6级声旁可靠性标注
├── P1-3: 形声字构件可视化增强
└── 验证：专家抽检标注质量

第4-5周 (P2)
├── P2-1: 繁体字形前端展示
├── P2-2: 古文字形扩展
├── P2-3: 说文解字增强
└── 验证：用户测试反馈

第6-8周 (P3)
├── P3-1: 王宁构形学分类体系
├── P3-2: 视觉语法规则引擎
└── 验证：学术顾问审核

第9-12周 (P4)
├── P4-1: 假字学习游戏
├── P4-2: 用户造字工坊
└── 验证：用户测试 + 教学实验
```

---

## 四、风险与依赖

| 风险 | 影响 | 缓解措施 |
|---|---|---|
| 古文字形 SVG 缺失大量汉字 | P2-2 无法全覆盖 | 优先覆盖6500通用规范字 + 回退GlyphWiki |
| 8级语义关系需要学术判断 | P1-1 标注质量 | 先标注500高频形声字 → 专家审核 → 批量推广 |
| 视觉语法规则边界模糊 | P3-2 准确性 | 参考王宁《汉字构形学导论》+ 专家评审 |
| 假字生成器可能生成真实罕见字 | P4-1 游戏有效性 | 与完整汉字库（含CJK Ext-B/C）做对比验证 |

---

## 五、文件改动预估

| 优先级 | 新增文件 | 修改文件 | 数据变更 |
|---|---|---|---|
| P0 | 1个（批量修正脚本） | 2-3个 | `hanzi-dict.json`（84条修正） |
| P1 | 2个（semanticLevels.ts, phoneticLevels.ts） | 4-5个 | 新增标注字段到hanzi-dict.json |
| P2 | 1个（SpatialRules.ts） | 3-4个 | 扩展字形目录、shuowen元数据 |
| P3 | 3个（visualGrammar.ts, modernTaxonomy.ts, 相关UI） | 3-4个 | 规则数据库JSON |
| P4 | 4个（PseudoCharGame, CharBuilder, 生成器引擎） | 2个 | 新JSON配置文件 |
