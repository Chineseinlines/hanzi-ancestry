# 字里行间 (Hanzi Ancestry) —— 项目技术文档

---

## 一、项目概述

**字里行间 (Hanzi Ancestry)** 是一个面向国际中文教育的汉字智能学习平台，以汉字构形学与《说文解字》六书理论为学术根基，融合知识图谱可视化、智能拆解算法与游戏化学习策略，将抽象的汉字学理论转化为可交互、可量化的数字化学习体验。

**网站地址**：https://chineseinlines.github.io/hanzi-ancestry/

**核心定位**：面向海外中文学习者、汉语国际教育教师及汉字文化爱好者的一站式汉字知识平台，解决传统汉字教学中"单字孤立学习、知识碎片化、形音义割裂"的核心痛点。

**核心数据规模**：覆盖 3500+ 常用汉字，每个汉字包含完整的六书分类、部件拆解、拼音标注与英文释义，构建了涵盖 9 种关系类型的多维汉字知识图谱。

---

## 二、技术栈总览

| 层级 | 选用技术 | 选型理由 |
|------|---------|---------|
| 前端框架 | React 19 + TypeScript | 类型安全的大型 SPA 开发，组件化架构 |
| 构建工具 | Vite 7 | 极速 HMR 开发体验，自定义插件扩展能力强 |
| 可视化引擎 | D3.js v7 | 力导向图布局、自定义 SVG 节点形状、精细交互动画 |
| 动效系统 | Framer Motion + GSAP | 声明式 React 动效 + 高性能 DOM 动画双引擎 |
| UI 组件 | shadcn/ui (Radix UI) + Tailwind CSS 3 | 无头组件 + 原子化 CSS，高度可定制 |
| 路由方案 | React Router v7 (Hash 路由) | GitHub Pages 兼容，无服务端路由需求 |
| 数据架构 | 静态 JSON + 客户端内存倒排索引 | 零服务端依赖，全静态托管 |
| 字体排版 | Noto Serif SC / Ma Shan Zheng / Inter | 中文衬线 + 毛笔书法 + 西文无衬线三字体系统 |
| 部署方案 | GitHub Actions → GitHub Pages | CI/CD 自动化，零成本静态托管 |

---

## 三、系统架构

本平台采用 **全静态客户端架构（Fully Static Client-Side Architecture）**，所有数据处理、计算与渲染均在浏览器端完成，无需任何服务端运行时。架构分为以下层次：

### 3.1 数据层（Data Layer）

- **静态 JSON 字库**：`hanzi-dict.json` 为每个汉字存储六书分类（象形/指事/会意/形声）、声旁/形旁标注、IDS 部件拆解串、英文释义及拼音数组
- **部件反查索引**：`hanzi-index.json` 建立"部件 → 包含该部件的所有汉字"倒排映射
- **文化数据**：`cultural.json` 存储字形演变描述、文化典故与关联词汇
- **《说文解字》数据**：`shuowen.json` 提供六书归类、字形结构解析与中英文摘要
- **简繁映射**：`simp-trad-map.json` 维护简体-繁体字形双向转换关系
- **英汉词索引**：`en-word-index.json`（15MB+）基于 CC-CEDICT 构建，支持英文关键词检索中文词汇

### 3.2 索引层（Index Layer）

数据加载时在内存中一次性构建 **7 个倒排索引**，全部系联计算在客户端完成：

| 索引名称 | 数据结构 | 功能 |
|---------|---------|------|
| `phoneticIndex` | `Map<声旁, Set<字>>` | 声旁 → 使用该声旁的所有汉字 |
| `semanticIndex` | `Map<形旁, Set<字>>` | 形旁 → 使用该形旁的所有汉字 |
| `pinyinIndex` | `Map<精确拼音, Set<字>>` | 完全同音字查询 |
| `pinyinNoToneIndex` | `Map<去调拼音, Set<字>>` | 近音字（忽略声调）查询 |
| `reverseIndex` | `Map<部件, string[]>` | 部件 → 包含该部件的所有汉字 |
| `radicalIndex` | `Map<部首, string[]>` | 同部首字族查询 |
| `structuralRank` | `Map<部件, 包含次数>` | 结构重要度：该字作为部件被引用的频次 |

### 3.3 计算层（Computation Layer）

所有汉字关系实时计算，无需预构建关系 JSON。核心计算模块包括：

- **六书驱动部件拆解**：根据字源分类采用差异化拆解策略
- **多维汉字系联网络**：9 种关系类型 × 4 级优先级 × 互斥去重
- **形音义三维亲密度评分**：加权几何平均 + 协同系数 + 频率修正
- **声旁可靠性评级**：声母-韵母双维度解析与对比
- **简繁异体去重**：多层次检测与智能过滤

### 3.4 展示层（Presentation Layer）

- **部件拆解图**（Decomposition Graph）：树形布局，语义/语音节点差异化形状与颜色
- **同源字关系网络图**（Cognate Graph）：力导向图，9 色关系编码，支持节点拖拽与缩放
- **字形演变时间轴**（Glyph Evolution）：甲骨文→金文→小篆→隶书→楷书五体横向滚动
- **笔画书写演示**（Stroke Order）：基于 hanzi-writer-data CDN 的 SVG 逐笔动画
- **IDS 文本拆解面板**：IDE 风格树形缩进文本视图

### 3.5 游戏层（Gamification Layer）

- **自适应汉字拼拆游戏引擎**：三种模式 × 程序化题目生成 × 智能干扰项抽取

---

## 四、核心技术详解

### 4.1 字源驱动部件拆解引擎（Etymology-Driven Decomposition Engine）

**技术背景**：传统汉字拆解方法采用统一的递归展开策略，忽视了不同六书类型汉字的结构差异——象形字不可拆分、指事字以抽象符号指示、形声字为形音复合、会意字为义素合成。统一的拆解策略会导致"伪字理"教学：将象形字强行拆解为无意义的笔画组合，或对声旁进行错误的语义解读。

**技术方案**：基于《说文解字》六书分类体系，对每个汉字执行差异化拆解策略：

| 六书类型 | 拆解策略 | 示例 |
|---------|---------|------|
| **象形字**（Pictographic） | 原子节点，不做任何拆分 | 「日」→ 保持完整，标注"太阳的象形描绘" |
| **指事字**（Indicative） | 原子节点，不做拆分 | 「上」→ 保持完整，标注"以短横指示上方位置" |
| **形声字**（Pictophonetic） | 单层展开：形旁 + 声旁为叶节点 | 「沐」→ 氵（形旁：水）+ 木（声旁：mù） |
| **会意字**（Ideographic） | 单层展开：从 IDS 串提取 CJK 部件 | 「森」→ 木 + 木 + 木（三重树木表示森林） |

**关键子模块：简体幽灵部件识别系统**

简体字中部分构件为草书楷化或同音替代产物，不具备任何造字意义上的表音或表意功能。系统维护一份手工标注的幽灵部件字典，涵盖草书简化（如「买」上部「乛」）、多字合并简化（如「发」合并「發」与「髮」）等典型情况。在拆解图谱中，幽灵部件以灰色虚线边框呈现，并附固定警示语：「该笔画/部件为简体简化衍生形态，无造字意义，无需拆分理解，建议整体记忆字形」，从根本上规避了"伪字理"教学误区。

**IDS 部件清洗规则**：从表意文字描述序列（Ideographic Description Sequence）中提取 CJK 汉字部件时，自动过滤纯笔画字符（一、丨、丿、丶、乙、亅、乀），仅保留具有独立语义的汉字级别部件。

---

### 4.2 多维汉字系联网络（Multi-Dimensional Character Relation Network）

**技术背景**：传统汉字教学中，字与字之间是孤立的，学习者无法感知汉字之间的渊源关系。实际上汉字系统内部存在丰富的结构化关联——分化字、同声旁字、同形旁字、同音字、反义对等。将这些隐性的学术知识显性化、可视化，是提升学习效率的关键。

**技术方案**：构建覆盖 **9 种关系类型、4 级优先级层次** 的多维系联网络。

**9 种关系类型定义**：

```
优先级 1（源流与对立）：
├── 源流分化（Differentiation）：B 以 A 为声旁，且 B 的部件拆解包含 A
│       例：「青」→「清」「晴」「请」「情」（青为声旁，分化加水/日/言/心形旁）
└── 反义对（Antonym）：高频反义配对
        例：「上↔下」「大↔小」「高↔低」「生↔死」等 57 组

优先级 2（字形结构）：
├── 同声旁族（Phonetic Family）：共享同一表音构件
├── 同形旁族（Semantic Family）：共享同一表意构件
└── 共享构件（Shared Components）：部件拆解中任意 CJK 部件重叠

优先级 3（字音关系）：
├── 构件包含（Contained In）：该字作为一个构件出现在其他字中
├── 同音字（Homophones）：声韵调完全相同
└── 近音字（Near-Homophones）：声韵相同但声调不同

优先级 4（补充层）：
└── 同部首族（Radical Family）：共享同一部首
```

**互斥去重机制**：每个汉字仅出现在其能归属的最高优先级关系中。例如「请」同时满足"源流分化"（以「青」分化出）和"同声旁族"（与「清」共享声旁「青」），则「请」仅出现在「青」的分化字列表中，不重复出现在同声旁族中。此设计避免了关系图谱中的信息冗余，确保每个关联词条都具有独特的信息价值。

**简繁异体智能去重**：采用三通道检测策略——
1. **通道一**：显式简繁映射表匹配（`simp-trad-map.json`）
2. **通道二**：同拼音 + 完全同释义 → 推断为异体关系
3. **通道三**：传统字标记级联传播（通过部首和字形部件两步检测）

保留高频常用字形（依据结构重要度评分），过滤异体冗余。

---

### 4.3 形音义三维亲密度评分算法（Tri-Dimensional Proximity Scoring Algorithm）

**技术背景**：汉字之间的关系并非二元的"有关/无关"，而是存在从强到弱的连续谱系。单纯罗列关联字无法帮助学习者判断哪些关联更值得关注。需要一个可量化的评分模型，从字形、字音、字义三个维度综合评估两个汉字之间的"亲密度"并排序输出。

**评分公式**：

```
totalScore = geometricMean(formScore, soundScore, meaningScore) × synergyBonus × frequencyModifier
```

**各维度评分细则**：

#### 字形维度（Form Score, 0–100, 权重 40%）

| 评分项 | 分值 | 说明 |
|--------|------|------|
| 共享构件比例 | 0–50 | 基于 Jaccard 相似度：`sharedCount / max(aComponents, bComponents) × 50` |
| 同部首 | +25 | 部首相同 |
| 同声旁 | +20 | 共享同一表音构件 |
| 目标字为候选字声旁（分化关系） | +15 | 源字作为声旁分化出新字 |
| 候选字为目标字的直接构件 | +10 | 构件包含关系（正向） |
| 目标字为候选字的直接构件 | +10 | 构件包含关系（反向） |

#### 字音维度（Sound Score, 0–100, 权重 35%）

| 匹配程度 | 分值 |
|---------|------|
| 完全同音（含声调） | 100 |
| 近音（同音节，不同声调） | 50 |
| 无音韵关联 | 0 |

#### 字义维度（Meaning Score, 0–100, 权重 25%）

| 评分项 | 分值 | 说明 |
|--------|------|------|
| 同形旁 | +40 | 共享表意构件（强语义信号） |
| 反义对 | +40 | 意义对立但同属一语义域 |
| 源流分化 | +30 | 从源字派生（强渊源关系） |
| 同部首 | +15 | 共享部首（弱语义信号） |
| 构件包含 | +10 | 结构上的包含关系 |

#### 协同系数（Synergy Bonus）

```
三个维度均 > 20 分 → synergyBonus = 1.5
仅两个维度 > 20 分 → synergyBonus = 1.2
其他情况         → synergyBonus = 1.0
```

逻辑：形音义多维度同时关联的汉字对具有更高学术价值，乘法加成确保其排名优先。

#### 频率修正因子（Frequency Modifier）

```
importance = structuralRank.get(c) + etymologyBonus + radicalFamilyBonus
frequencyModifier = min(1.35, 0.35 + log₂(importance + 1) / 6)
```

其中：
- `structuralRank`：该字作为构件被其他字引用的次数（**结构基础性**）
- `etymologyBonus`：该字是否具有字源学数据（**学术完备性**）+5 分
- `radicalFamilyBonus`：同部首字族的 log₂ 规模（**部首代表性**）

频率修正因子范围为 `[0.35, 1.35]`——常用基础字权重被适当提升，生僻字权重被合理抑制。

**算法优势**：
- **几何平均天然惩罚单维度匹配**：如某对字仅在字形上高度相似但字音字义毫无关联（几何平均 = ³√(90×15×15) ≈ 27），得分远低于三个维度均有中等关联的字对（几何平均 = ³√(50×50×30) ≈ 42），避免了"形似义无关"的浅层匹配占据高分位
- **协同乘法而非加法**：确保高分结果具备学术上的多维度支撑

---

### 4.4 声旁可靠性三色评级系统（Phonetic Reliability Rating System）

**技术背景**：约 80% 的现代汉字为形声字，声旁提供了重要的读音线索。但由于古今音变，部分声旁已完全失效（如「汤」tāng 的声旁为「昜」yáng）、部分部分失效（如「轩」xuān 的声旁为「干」gān）。不加区分地标注声旁关系可能导致学习者"读字读半边"，产生系统性发音偏误。

**技术方案**：内置完整的汉语拼音声母-韵母解析器，对每个形声字进行声旁可靠性评估。

**解析器能力**：
- 支持数字调号（`he2`→hé）与变音符号（`hé`）双向输入
- 零声母识别（如「安」ān → 声母=空，韵母=an）
- ü 特殊处理（`ǘ`→`v`）
- 全部 21 个声母的识别与匹配

**三色评级标准**：

| 评级 | 颜色 | 条件 | 标签 | 教学含义 |
|------|------|------|------|---------|
| 🟢 准确 | 绿色 `#2E7D32` | 声母 AND 韵母均匹配 | `声✓` | 可直接参考声旁读音 |
| 🟡 近似 | 黄色 `#F57F17` | 声母 OR 韵母其一匹配 | `声~` | 仅可部分参考 |
| 🔴 失效 | 红色 `#C62828` | 声韵均不匹配 | `声✗` | 古今音变，不可凭偏旁猜读 |

**示例**：

```
「芳」fāng → 声旁「方」fāng → 🟢 声韵一致（同 f-ang）
「轩」xuān → 声旁「干」gān  → 🟡 韵母近似（uān vs ān 部分对应）
「汤」tāng → 声旁「昜」yáng → 🔴 完全失效（t-ang vs y-ang，古今音变）
```

**可视化呈现**：在部件拆解图中，声旁节点上方悬挂彩色标签，菱形节点边框颜色随评级动态变化，配合详细中文提示（如"声韵一致，可直接参考声旁读音"），为学习者提供即时的发音参考可靠性评估。

---

### 4.5 轻量化静态知识图谱引擎（Static Multi-Index In-Memory Graph Database）

**技术背景**：传统知识图谱方案（Neo4j、JanusGraph 等）需要独立的图数据库服务端，部署复杂、运维成本高，不适合面向 C 端用户的轻量级学习平台。本项目需要一种零服务端依赖的解决方案。

**技术方案**：设计纯客户端内存图数据库，在数据加载时一次性构建 7 个 Hash 倒排索引，所有汉字间关系在浏览器端以 O(1) 时间复杂度实时查表计算。

**索引构建流程**：

```
1. 加载 hanzi-dict.json（汉字字典，3500+ 条）
2. 遍历字典，为每个字提取：
   ├── phonetic → 写入 phoneticIndex
   ├── semantic → 写入 semanticIndex
   ├── pinyin[] → 写入 pinyinIndex + pinyinNoToneIndex
   └── radical  → 写入 radicalIndex
3. 加载 hanzi-index.json（部件反查表）
   ├── 写入 reverseIndex
   └── 计算 structuralRank（引用计数）
4. 加载 simp-trad-map.json
   ├── 建立正向映射（简→繁）
   ├── 建立反向映射（繁→简）
   └── 构建传统字标记集（两步级联传播）
```

**懒加载设计**：15MB+ 英汉词索引（CC-CEDICT 级别）仅在用户首次进行英文搜索时触发加载，确保首屏渲染不受大数据量影响，核心功能秒开。

**架构优势**：
- **零服务端依赖**：可托管于 GitHub Pages 等静态文件服务
- **零部署成本**：无需购买云服务器或数据库实例
- **零查询延迟**：所有查询为内存 Hash 查表，无网络往返
- **离线可用潜力**：所有数据为静态 JSON，可扩展 PWA 离线模式

---

### 4.6 字形图片智能缓存代理（Vite Glyph Cache Proxy Plugin）

**技术背景**：古文字字形图片（甲骨文、金文、小篆等）需要从第三方字源网站获取，直接前端请求面临跨域限制，且频繁外部请求影响加载速度与稳定性。

**技术方案**：开发自定义 Vite 插件，拦截 `/glyphs/{script}/{hex}.svg` 请求，实现"按需爬取 + 本地缓存 + 降级回退"三级策略。

**工作流程**：

```
请求 /glyphs/oracle/4E00.svg
       │
       ▼
  ┌─ 检查 public/glyphs/oracle/4E00.svg ─┐
  │                                       │
  ▼ 存在                                  ▼ 不存在
直接返回                          ┌─ 转换 hex→Unicode 字符
                                  │
                                  ▼
                           爬取 zdic.net 页面
                                  │
                                  ▼
                           解析 HTML 提取 SVG URL
                                  │
                               ┌──┴──┐
                               ▼     ▼
                            成功   失败（404）
                               │     │
                               ▼     ▼
                        写入缓存   前端降级：
                        返回图片   使用系统字体渲染
```

**支持的四种书体**：

| 书体 | 时期 | zdic 目录 | 示例 |
|------|------|----------|------|
| 甲骨文 | c. 1250 BCE | `jiaguwen` | 殷墟卜辞刻写体 |
| 金文 | c. 1046 BCE | `jinwen` | 青铜器铭文 |
| 小篆 | c. 221 BCE | `xiaozhuan` | 秦统一规范字体 |
| 隶书 | c. 200 CE | `lishu` / `qinwenzi` | 汉代隶变过渡体 |

**技术细节**：
- 完整的 HTTP 客户端模拟：User-Agent、Referer、Accept 头伪装
- 重定向跟随（301/302/307/308）
- MIME 类型自动检测（PNG/GIF/JPEG/SVG）
- 24 小时缓存控制头

---

### 4.7 自适应汉字拼拆游戏引擎（Adaptive Character Puzzle Game Engine）

**技术背景**：游戏化学习需要题目自动生成能力，且题目质量直接影响学习效果。干扰项过于简单则无挑战性，过于随机则无教学价值。需要一种"教学价值导向"的题目生成策略。

**技术方案**：三种游戏模式，题目均从知识图谱中程序化生成，干扰项具备合理的迷惑性。

#### 模式一：拆字挑战（Decompose Mode）

- 展示目标汉字，学习者从候选中选出正确的组成部件
- **干扰项来源**：从目标字的同源字（共享声旁/形旁的字）中提取真实部件
- **教学意义**：干扰项均为真实存在的汉字部件，避免因选项荒谬而降低思维训练价值

#### 模式二：拼字挑战（Assemble Mode）

- 展示一组部件，学习者判断它们能组成哪个汉字
- **候选字生成**：查找共享这些部件的字符交集 → 筛选完全匹配 → 生成干扰项（仅共享部分部件的字）
- **降级策略**：当精确匹配失败时，回退到目标字 + 同源字随机排列

#### 模式三：部件连连看（Match Mode）

- 左侧列出部件，右侧列出候选字，学习者进行双向匹配
- **约束条件**：每个候选字确实包含其匹配的部件
- **验证机制**：所有匹配完成后统一校验

#### 通用机制

- **连击奖励**：streak ≥ 3 时，每题获得 `streak × 2` 的加分
- **常错即时反馈**：错误时立即显示正确答案 + 对应的部件组成
- **做题范围**：限 6500 通用规范汉字（一二级），避免生僻字影响学习体验
- **弹性动画**：Framer Motion 驱动的题目切换动效 + 正确/错误状态的色彩反馈

---

### 4.8 交互式汉字知识图谱可视化（Interactive Knowledge Graph Visualization）

**技术方案**：基于 D3.js v7 力导向图，针对汉字教学场景的专项可视化设计。

**节点设计**：
- **圆形节点**：核心字、形旁节点、会意节点、普通叶节点
- **菱形节点**（旋转正方形）：声旁节点 —— 实现图形-功能双重编码，色盲友好
- **灰色虚线节点**：幽灵部件（简体简化产生）
- 节点大小与关系强度正相关

**边设计**：
- **实线**：普通关联关系
- **虚线**：反义关系
- 颜色与关系类型编码一致
- 边宽度与共享构件数量正相关

**交互设计**：
- **悬停高亮**：hover 节点 → 关联边加粗变红 + 非关联节点淡出至 40% 透明度
- **拖拽解耦**：节点可自由拖拽，释放后回归力导向平衡
- **缩放平移**：0.3×–3× 缩放范围，鼠标滚轮 + 拖拽平移
- **双击跳转**：双击任意节点以该字为中心重新展开系联网络
- **单击导航**：单击任意节点跳转至该字的详情页面
- **弹性入场动画**：`d3.easeBackOut.overshoot(1.2)` 实现节点从 0 弹性缩放到目标半径

---

## 五、数据来源与处理管线

### 5.1 数据来源

| 数据项 | 来源 | 说明 |
|--------|------|------|
| 汉字字库 | Make Me a Hanzi | 开源汉字数据库，含六书分类、IDS 拆解、英文释义 |
| 英汉词索引 | CC-CEDICT | 开源中英双语词典（15MB+），构建英文关键词检索 |
| 古文字字形 | zdic.net（汉典） | 甲骨文/金文/小篆/隶书 SVG 图片，按需爬取 |
| 楷书笔画 | hanzi-writer-data CDN | 逐笔 SVG 路径数据，支持笔画书写动画 |
| 《说文解字》 | 手工整理标注 | 六书归类、字形结构解析、中英文双语摘要 |
| 简繁映射 | 开源简繁对应表 | 简体-繁体双向映射，辅助字形变体去重 |
| 文化典故 | 手工整理 | 字形演变描述、历史文化典故、关联词汇 |

### 5.2 数据处理管线

```
原始字典文件 (dictionary_raw.txt)
        │
        ▼
  数据清洗脚本 → 标准化 JSON (hanzi-dict.json)
        │
        ▼
  关系计算脚本 (build-relations.ts)
  ├── 构建 7 个倒排索引
  ├── 计算每个字的 9 种关系
  └── 生成笔画缓存 (cache-glyphs.ts)
        │
        ▼
  部署到 public/ 目录 → Vite 构建打包 → GitHub Pages
```

---

## 六、项目部署

### 6.1 部署架构

```
GitHub 仓库 (Chineseinlines/hanzi-ancestry)
        │
        │ git push (main 分支)
        ▼
GitHub Actions 自动触发
├── actions/checkout → 拉取代码
├── actions/setup-node (v20) → 配置 Node.js 环境
├── npm ci → 安装依赖
├── npm run build → tsc 编译 + Vite 构建 + 生成 404.html
├── actions/configure-pages → 配置 Pages 环境
├── actions/upload-pages-artifact → 上传 dist/ 产物
└── actions/deploy-pages → 部署到 GitHub Pages
        │
        ▼
https://chineseinlines.github.io/hanzi-ancestry/
```

### 6.2 SPA 路由兼容

- 构建时自动复制 `index.html` → `404.html`，利用 GitHub Pages 的 404 回退机制实现 SPA 客户端路由
- `vite.config.ts` 中配置 `base: '/hanzi-ancestry/'` 确保所有资源路径在子路径下正确引用
- 全局 404 页面重定向脚本处理直接访问子路由的情况

---

## 七、功能模块总览

| 页面 | 路由 | 核心功能 |
|------|------|---------|
| **首页** | `/` | 三模搜索（汉字/拼音/英文关键词）、快速入口、特色功能区导航 |
| **汉字探索** | `/explore` | 部件拆解图 + 同源字关系网络图并排展示、IDS 文本拆解面板 |
| **汉字详情** | `/detail` | 六书分类描述、字形演变时间轴、笔画书写动画、关联汉字卡片、趣味练习游戏 |
| **关联汉字** | `/relations` | 形音义三维评分排序、多维筛选、标签分组 |
| **学习中心** | `/learn` | 汉字构形学理论讲解、IDS 参考表、字源系联方法论 |
| **趣味游戏** | `/games` | 拆字/拼字/连连看三种模式、古文字辨识 |
| **关于** | `/about` | 数据来源说明、学术依据 |

---

## 八、技术创新总结

| 序号 | 技术名称 | 创新点 | 对应章节 |
|------|---------|--------|---------|
| 1 | 字源驱动部件拆解引擎 | 六书分类 × 差异化拆解策略 + 幽灵部件识别 | §4.1 |
| 2 | 多维汉字系联网络 | 9 种关系 × 4 级优先级 × 互斥去重 × 简繁过滤 | §4.2 |
| 3 | 形音义三维亲密度评分算法 | 加权几何平均 × 协同系数 × 频率修正 | §4.3 |
| 4 | 声旁可靠性三色评级系统 | 声母-韵母双维度解析 + 古今音变识别 | §4.4 |
| 5 | 轻量化静态知识图谱引擎 | 7 索引客户端内存图数据库 + 关系即计算 | §4.5 |
| 6 | 字形图片智能缓存代理 | 按需爬取 + 本地缓存 + 字体降级三级策略 | §4.6 |
| 7 | 自适应汉字拼拆游戏引擎 | 知识图谱驱动的程序化题目生成 + 智能干扰项 | §4.7 |
| 8 | 交互式知识图谱可视化 | D3 力导向图 + 声旁菱形编码 + 多模态交互 | §4.8 |

---

*文档生成日期：2026-06-03*
